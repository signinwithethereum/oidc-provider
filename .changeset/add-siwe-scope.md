---
"@signinwithethereum/oidc-provider": minor
---

Add `siwe` scope that includes `siwe_message` and `siwe_signature` claims in the ID token, allowing relying parties to obtain cryptographic proof of the user's SIWE authentication.
